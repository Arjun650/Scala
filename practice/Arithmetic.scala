object Arithmetic{
    def main(args: Array[String]): Unit ={
        var a : Int = 10; 
        var b : Int = 20; 

        println("a + b = " + (a + b)); 
        println("a - b = " + (a - b)); 
        println("a * b = " + ( a * b)); 
        println("a / b = " + ( a / b)); 
    }
}